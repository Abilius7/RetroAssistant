import { CambioFechaPipe } from './cambio-fecha.pipe';

describe('CambioFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new CambioFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
